% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sales_ts_logic.R
\name{sales_ts_logic}
\alias{sales_ts_logic}
\title{Run sales time series analysis logic}
\usage{
sales_ts_logic(
  data,
  city = NULL,
  state = NULL,
  store_type = NULL,
  cluster = NULL,
  store_nbr = NULL,
  start_date = NULL,
  end_date = NULL,
  group_vars = c("store_nbr", "family"),
  plot_group_var = "family",
  aggregate_period = c("month", "week", "day"),
  moving_average_window = 7,
  add_moving_average = FALSE,
  run_metrics = TRUE,
  run_plot = TRUE
)
}
\arguments{
\item{data}{A named list returned by [load_sales_data()] or
[clean_sales_ts()]. It should contain `train` and `stores` tibbles.}

\item{city}{Character vector or `NULL`. Optional city filter.}

\item{state}{Character vector or `NULL`. Optional state filter.}

\item{store_type}{Character vector or `NULL`. Optional store type filter.}

\item{cluster}{Integer or numeric vector or `NULL`. Optional cluster filter.}

\item{store_nbr}{Integer or numeric vector or `NULL`. Optional store number
filter.}

\item{start_date}{Date or character. Optional start date filter.}

\item{end_date}{Date or character. Optional end date filter.}

\item{group_vars}{Character vector or `NULL`. Grouping variables passed to
[compute_sales_metrics()]. Defaults to `c("store_nbr", "family")`.}

\item{plot_group_var}{Character or `NULL`. Grouping variable passed to
[plot_sales_trends()]. Defaults to `"family"`.}

\item{aggregate_period}{Character. Time aggregation period passed to
[plot_sales_trends()]. One of `"day"`, `"week"`, or `"month"`.
Defaults to `"month"`.}

\item{moving_average_window}{Integer. Window size used by metrics and,
optionally, the plot. Defaults to `7`.}

\item{add_moving_average}{Logical. Should the trend plot include a moving
average line? Defaults to `FALSE`.}

\item{run_metrics}{Logical. Should sales metrics be computed?
Defaults to `TRUE`.}

\item{run_plot}{Logical. Should a sales trend plot be created?
Defaults to `TRUE`.}
}
\value{
A named list with:
\describe{
  \item{filtered_data}{Filtered sales data object with `train`, `stores`,
  and optionally `holidays_events`.}
  \item{metrics}{Metrics returned by [compute_sales_metrics()], or `NULL`
  if `run_metrics = FALSE`.}
  \item{plot}{A `ggplot` object returned by [plot_sales_trends()], or `NULL`
  if `run_plot = FALSE`.}
  \item{filters}{A list describing filters used in the analysis.}
}
}
\description{
Runs a higher-level sales time series analysis workflow on selected stores,
metadata groups, and time periods.
}
\details{
The function filters sales data by store metadata such as city, state,
store type, cluster, store number, and date range. It then optionally
computes sales metrics using [compute_sales_metrics()] and creates a sales
trend plot using [plot_sales_trends()].


This function is intended as a convenience wrapper for repeated analyses
on selected subsets of stores and time periods.

Filtering by `city`, `state`, `store_type`, and `cluster` requires the
`stores` metadata table. The function uses the internal sales schema
defined by `sales_schema()`, so column names are resolved dynamically.
}
\examples{
\dontrun{
sales_data <- load_sales_data("data/")
clean_data <- clean_sales_ts(sales_data)

result <- sales_ts_logic(
  clean_data,
  city = "Quito",
  store_type = "D",
  start_date = "2016-01-01",
  end_date = "2017-08-15",
  group_vars = c("store_nbr", "family"),
  plot_group_var = "family",
  aggregate_period = "month"
)

result$metrics
result$plot
}

}
